# Copy to `.env` inside TDOP-infra and adjust values.
# REQUIRED by both docker-compose.yml and docker-compose.dev.yml.
# For local development only — generate a strong password anywhere else.

POSTGRES_DB=tdop
POSTGRES_USER=tdop
POSTGRES_PASSWORD=